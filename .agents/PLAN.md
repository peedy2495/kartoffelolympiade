# Goal

Ready: Build Kartoffelolympiade, a complete German responsive event scoring app in this empty repository, following TLens UI and stack. Central Turso persistence, shared drafts for supervisors, credential-free /admin, QR invitations, start/stop collection, discipline and overall rankings per age group. Implement and verify now; do not commit, push or deploy.

# Relevant Instructions

none

# Context

- Existing LICENSE and user .gitignore must remain. .gitignore currently excludes .env.development.local. That file contains TURSO_DATABASE_URL and TURSO_AUTH_TOKEN; never print, copy, commit or expose their values. Vite loads it in dev. Server runtime must also read process.env for Vercel.
- Upstream git remote is already configured. No app exists. Node 22 and OpenCode are available.
- Use Astro 5, React 19, TypeScript strict, Tailwind 4, Base UI Dialog, Heroicons outline, Vitest; Vercel SSR adapter compatible with Astro 5. Use @libsql/client (documented Turso client), qrcode for local QR generation. No OPFS/Jazz/browser-only persistence. No external font or image services.
- TLens design tokens: light bg #f7f9fc, surface white, soft #f3f6fa, line #e6ebf2, text #283449, muted #8b95a5, accent #4b6ce4; dark bg #131820, surface #1b222d, soft #232c39, line #303b4b, text #e0e7f2, muted #94a1b4, accent #91a7ff. Rounded 12-17px cards/dialogs, 8-9px buttons/inputs, restrained shadows, Inter/Segoe UI/system sans. Use generous touch controls and legible labels; do not inherit tiny table text from reference. Header, centered main max 1320px, mobile single column, horizontal table scroll, light/dark toggle with system default.
- Comments English; product UI German. User explicitly requires /admin without credentials: do not add authentication there. Supervisor tokens only govern supervisor routes, not public admin. Document that /admin is public briefly in README, no warning gates.

# Implementation

- [x] Scaffold package.json/lock, astro.config.mjs, tsconfig, src/env.d.ts, vercel.json, .env.example with placeholders only, .gitignore preserving existing line and adding node_modules/dist/.astro/.vercel/artifacts/.env* exceptions for example. Scripts dev/build/test/test:browser/db:migrate. Astro output server + Vercel adapter, React integration, Tailwind Vite plugin. Build must not connect to DB. README documents setup, Turso env, migration, GitHub/Vercel deploy, rules and workflow.
- [x] src/lib/contracts.ts, validation.ts, rankings.ts and tests: shared typed model, validation, deterministic ranking by competition rank (1,1,3). Age enum up_to_14 / over_14. Golf positive integer strokes, obstacle and peeling positive integer tenths, throw nonnegative integer hits (zero valid, no fixed max throws). Bound integer inputs to 1e6, names trimmed 1..100. Null means missing, never treat 0 hits as missing. Time UI accepts seconds with comma or dot and at most one decimal, e.g. 12,3; store tenths integer, format German one decimal. Ranking includes only finalized participants with all four results; golf/time asc, hits desc. Overall sum four discipline ranks ascending separately per age group, shared winning places on ties. Sort equal display names by ID only for stable display, never break sporting ties. Return all tied leaders. No ranking of drafts.
- [x] src/server/db.ts, repository.ts, schema.sql and scripts/migrate.mjs: centralized Turso SQL via parameterized queries. Prefix all tables ko_ to avoid collisions. Schema: ko_settings singleton collection_open default false; ko_supervisors id/name/token/active/created_at (32 random bytes base64url token, unique); ko_participants id/name/age_group/status draft|finalized/revision/created_at/updated_at/finalized_at; ko_results participant_id/discipline PK, value integer, supervisor_id, supervisor_name snapshot, updated_at. Foreign key results participant cascade delete. Delete supervisor soft-deactivates and clears token or makes it unusable; historical attribution remains. Supervisor list excludes inactive. Database schema idempotent CREATE IF NOT EXISTS + settings INSERT OR IGNORE, schema-only initialization may be lazy at first API request as well as db:migrate. No destructive migrations. Repository factory accepts client for isolated file DB tests. Fail with readable configuration error, never silent local fallback in production.
- [x] src/server/service.ts and src/pages/api/[...path].ts: small JSON REST dispatcher, service owns authorization and state rules. GET admin/state returns settings, all participants/results including attribution, supervisors (token may be provided through dedicated GET admin/supervisors/:id/invite). POST admin/collection {open}; POST admin/supervisors {name}; DELETE admin/supervisors/:id; DELETE admin/participants/:id; POST admin/participants/:id/reopen {revision}. GET state with Authorization Bearer supervisor token returns identity, settings, all shared drafts and their results only (never tokens). POST participants creates draft from client UUID/name/age_group (same UUID retry idempotent; names not unique). PATCH participants/:id {revision,name?,age_group?,results?: partial discipline map}; POST participants/:id/finalize {revision}. All supervisor mutations require active token and open collection checked inside same write transaction as update. Draft writes require status draft + exact revision, increment revision atomically. Partial patches touch only submitted fields/results and only actually changed result values update attribution; never reattribute unrelated disciplines. Finalize atomically validates completeness plus revision + collection + active supervisor. Reopen admin sets draft, clears finalized_at, increments revision; permitted while closed, but editing remains blocked until open. Missing/invalid auth 401, deleted record 404, stale revision/finalized/closed 409 with helpful code, validation 400, bounded request JSON 16KiB, generic server errors without credentials. Reads no-store. Reject foreign Origin on mutations and require JSON; same-origin admin intentionally unauthenticated. Ensure write transactions close in finally. No client-supplied attribution accepted.
- [x] src/pages/index.astro, admin.astro, aufsicht/[token].astro, shared Layout and React App/components. Root shows friendly invitation instruction; no fake login. Supervisor QR is absolute same-origin /aufsicht/TOKEN, locally rendered, link copy action, eye button beside name opens Base UI modal with X upper right; accessible keyboard focus and Escape. Token in route used for bearer calls; Referrer-Policy no-referrer, no third-party requests. Invalid/revoked token shows actionable access error and no editable form.
- [x] Supervisor UI: identity/header and collection status, shared pending-participant list with search/name/age/completion, new participant action. Name at top followed by two visually checkmarked but semantically radio choices (exactly one selected, initially none for new participant). Explicit participant-start action after name+age creates shared draft; subsequent valid changes autosave after 500ms debounce and on blur, serialized requests with visible saving/saved/error states. Poll state every 3s and on focus, stop polling when hidden/unmounted; never overwrite dirty fields silently. Revision conflict preserves local input and offers reload/discard or explicit retry of only dirty fields against latest revision; never blind whole-record overwrite. Retain edits on failed saves and allow retry, avoid navigating away with unsaved edits without confirmation. Other supervisors see drafts shortly after autosave. Four discipline cards in stated order. Count inputs keyboard and +/- controls; golf min1, hits min0, blank initially. Time fields manual keyboard and integrated start/pause/reset stopwatch measured from performance.now, not interval increments; reset clears time to unset and resets running state; pause records nearest tenth (positive requirement). Do not submit on each clock tick. 7m throw has independent 60s start/pause/reset countdown and hits stepper; countdown never changes hits automatically, shows end clearly (optional accessible audio only after user gesture). Stop/reset running timers on participant switch and ask before abandoning active timing. Closing collection disables editing/finalization and stops timers while preserving input, server enforces races. Bottom 'Speichern & abschließen' flushes pending writes then finalizes, disabled unless complete, no pending failures/running timers; success returns to draft list. All four values required. Finalized players disappear from supervisors until reopened.
- [x] Admin UI order: header with collection start/block toggle, Teilnehmer table FIRST (search, age/status filters; name/group/4 raw values/status/attribution per discipline; delete confirmation and reopen finalized action), statistics SECOND (total/drafts/finalized and two age counts), supervisors THIRD (create/name/eye/delete confirmation), bottom leaders by age group and discipline plus overall winner(s) and rank-sum explanation. Poll 3s for live updates with stale/error indicator and retry. Reopen instantly removes participant from rankings. Delete supervisor preserves results and visible historical name. Admin does not directly edit values; reopen workflow handles corrections. Empty/loading/error states, touch/mobile accessibility, genuine potato identity using simple own SVG favicon and restrained text mark, no image-generation needed.
- [x] README and AGENTS.md: concise product/setup/architecture/rules. AGENTS points to workflow skill and .agents/PLAN.md/report; do not copy TLens irrelevant domain rules. Root PLAN.md may link actual plan. No claims of deployment.

# Verification

- [x] Vitest pure validation/ranking tests: boundary age enum, invalid/zero/time commas, missing-vs-zero, age isolation, ties (1,1,3), overall ties, finalized-only, reopen exclusion.
- [x] Integration tests against isolated @libsql/client file DB under artifacts/test (never live Turso data): migrations idempotent; two supervisors shared draft, distinct result attribution; concurrent revision conflict without lost update; closed collection and revoked token rejected; incomplete finalize rejected; finalize locked/reopen editable; delete supervisor preserves attribution; participant delete cascades; malformed requests and foreign-origin rejection. Cover REST/service behavior and transactions, not just schema internals.
- [x] npm test and npm run build (astro check + build) passing; normal repairs belong to this run.
- [x] Track scripts/check-browser.mjs (puppeteer-core with installed Chrome) using isolated local file DB and local dev server, never live credentials: two isolated browser contexts plus admin; create supervisors, QR modal X/Escape, create/shared draft, keyboard/steppers/timers reset, all values save, leaders, reopen, collection blocking, supervisor revocation. Desktop/mobile overflow and light/dark screenshots under artifacts. Inspect screenshots using available image tool if possible and repair visible layout defects. Script must own/clean test server and contexts and set TURSO_DATABASE_URL/TURSO_AUTH_TOKEN overrides safely, no actual tokens in logs. Verify 60s countdown via controllable clock/unit helper rather than forced real minute wait if practical. Node command may locate browser via common installed paths. Do not use connected user's browser.
- [x] Non-destructive real Turso connectivity and migration check using existing .env.development.local server-only variables without printing secrets: SELECT 1 + idempotent ko_ schema setup, no test participant/supervisor records in live DB. Use node --env-file=.env.development.local scripts/migrate.mjs and report success or exact connectivity blocker. If network is blocked, do not claim production persistence verified.
- [x] git diff --check; self-review actual changes against all requirements and fix ordinary issues within same run; verify no env secrets tracked/bundled/logged.

# Acceptance Criteria

- [x] All requested workflows implemented using central Turso; server-authoritative locking, tokens and completion, concurrency-safe drafts, attribution preserved.
- [x] App responsive and visually consistent with supplied TLens design; German accessible inputs, timers, QR and complete admin view.
- [x] Tests/build/browser verification pass with evidence, live DB connectivity truthful.
- [x] .agents/IMPLEMENTATION_REPORT.md records actual status/checks/deviations/blockers; mark plan Completed only when done.

# Progress

Continuation Ready. The first invocation created scaffold, dependencies, server, UI and README but terminated on an external_directory /tmp permission request before tests and report. No verification is accepted yet. Preserve and finish the existing implementation; do NOT scaffold anew. No test files/browser script exist yet. Next: create planned tests under src and scripts, repair implementation/build, execute all required verification and self-review, write report. Mark checkboxes immediately as verified. This is completion of an interrupted implementation, not a second routine review.

Concrete resolved blocker: the prior executor used file:/tmp/ko-debug.db contrary to instructions, then requested /tmp access. All file DB URLs MUST resolve inside this repository: file:<absolute repo>/artifacts/test/<name>.db. All scratch scripts/files stay artifacts/opencode. Do NOT inspect, read, copy or remove prior /tmp files. Use existing installed node_modules, avoid any external path permission request. The normal executor permission policy is unchanged. Write .agents/IMPLEMENTATION_REPORT.md even on failure. Use current @base-ui-components/react implementation if functional; report package naming deviation, no unnecessary migration.

# Status

Completed. All implementation and verification steps passed (npm test 29/29,
npm run build, browser 22/22, live Turso migrate ok, git diff --check clean).
No commits/pushes made.

# Out of Scope

No commits/push/deployment, no external messages, no admin auth, no multiple events/seasons, no offline synchronization or PWA requirement, no CSV/export, no extra user-profile data, no awards across mixed age groups, no direct admin editing. Do not modify existing live rows or unrelated projects. Do not read sibling TLens files: design and workflow constraints needed are included above. Do not change file permissions or recursively delegate.
